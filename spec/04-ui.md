# 04 - UI

> Status: ACTIVE — the UI contract. Per-screen detail and the canvas mapping live in the
> [design handoff](../docs/plans/2026-09-22-001-feat-iphone-app-design-handoff.md); the canvas source is in
> [`docs/design/2026-09-21-iphone-canvas/`](../docs/design/2026-09-21-iphone-canvas/canvas.json).

## Design tokens (`ChirpUI.Tokens`)

| Token | Value | Use |
|---|---|---|
| accent (coral) | `#E86B3B` | Primary actions, Dictate button, progress |
| accentInk | `#BE4E26` | Links, accent text on light fills |
| ground | `#FAFAF7` | Screen background (warm off-white) |
| surface | `#FFFFFF` | Cards, rows |
| border | `#E8E8E0` | Card and row borders |
| ink | `#1A1A1A` | Primary text |
| secondary | `#6B6B6B` | Secondary text, meta |
| tint | `#FFF0EB` | Selected chip, Dictate card, current paragraph |
| success | `#33A854` | "On" toggles, level meters, ready dots |
| rosette | `#59A659` | Meeting capture rosette |
| recordRed | `#E64D42` | Recording dot |
| stopRed | `#C9342B` | Stop button |
| night (dictation) | `#141417` | Dictating screen background |
| coverNight | `#16211D` | Seed-of-Life cover field |

Speaker palette (dot / ink): blue `#3382D6` / `#2A6CB5`, purple `#B854A3` / `#9A3F87`, green `#299975` / `#1E7B5D`,
amber `#D18524` / `#8F5A12`. Speakers cycle through the four in order of first speech.

Radii: 14 (rows, small cards) · 16 (cards) · 20 (large cards) · 24 (hero card). Type: **SF Pro Rounded** bold for
headlines and titles (`Tokens.Font.rounded`), **SF Pro** for body text. Numbers that change (timers, times,
percentages) use tabular figures.

Dark mode: the canvas defines a light palette only (the Dictating screen is dark by design). A dark palette is an
M8 design task for the owner; until it exists, do not invent one in code.

## Structure

Four tabs, in this order, with SF Symbols: **Capture** (`waveform`), **Library** (`square.grid.2x2`),
**Transforms** (`sparkles`), **Settings** (`gearshape`). Transcript is pushed from Capture's Recent list and from
Library.

| Screen | M1 | Notes |
|---|---|---|
| Capture | Real | Header (Parakeet mark, "Parakeet", "On device" chip), Dictate card (placeholder action), Paste a link (placeholder), Import audio (real), Record Meeting row (placeholder), Recent (3 rows, live progress) |
| Library | Real | Search, filter chips, day sections, rows with covers, swipe-to-delete with confirmation |
| Transcript | Real (Transcript tab) | Header, player bar, speaker paragraphs, Copy / Share / Transform bar; Notes and Ask tabs are placeholders |
| Settings | Real | Speech model, model version, language, speaker labels, clean-up, privacy statement, About, DEBUG Diagnostics |
| Transforms | Placeholder list | The canvas's Transform list, each with a "Milestone M4" badge |
| Dictating, Meeting, Ask, Transform share sheet | Not built | Documented in the handoff for M2, M3, M4, M8 |

## Components (`ChirpUI`)

`Card`, `SeedOfLifeCover`, `RosetteMark`, `ParakeetMark` (the canvas's SVG path as a SwiftUI `Shape`),
`StatusChip`, `SpeakerDot`. Shared app views: `NotBuiltYetSheet(title:milestone:summary:)`, `PlayerBar`,
`AboutSection`.

## Rules

- **Honest UI.** Progress comes only from real work (pipeline stages and engine callbacks). No timers that fake
  progress, no demo rows, no buttons that pretend. Unbuilt surfaces open `NotBuiltYetSheet` naming the milestone.
- **Errors are actionable.** A failed row shows the error in red with Retry; a missing model shows a banner with a
  button to Settings.
- **Copy corrections to the canvas** (binding):
  - "Hold the Action Button" → **"Press the Action Button"**. Third-party apps get no key-up event from the Action
    Button, so hold-to-talk is impossible.
  - The meeting **"Room"** meter has no iOS source: iPhone apps cannot capture other apps' or system audio. It
    becomes a second built-in-mic / voice-isolation reading or is dropped (decided in M3).
  - Canvas numbers are illustrative: show real model sizes on disk, not "620 MB"; show the language the engine
    actually supports ("Automatic (25 languages)" for v3), not "English (US)"; clean-up is Raw/Clean, not
    "Balanced".
- **Build identity is visible**: Settings → About shows version (build), commit, branch, date and a copy button.
- **Accessibility**: every icon-only control has a label; tap targets are at least 44 pt; text uses Dynamic Type
  styles scaled from the canvas sizes. A full accessibility pass is M8.
- **iPhone portrait first.** The target is universal (iPhone and iPad); the iPad layout is M8.

## How to verify a UI change

```bash
cd /Users/ama/Documents/GitHub/iChirp
scripts/run_sim.sh
mkdir -p .build/shots && xcrun simctl io booted screenshot .build/shots/capture.png
```

Compare the screenshot with the canvas artboard for that screen (layout, colors, copy). XcodeBuildMCP
(`screenshot`, `snapshot_ui`) and the iOS Simulator MCP work too.
