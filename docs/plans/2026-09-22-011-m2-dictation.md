# Plan: M2 — Dictation (Action Button, Live Activity, live preview, final Parakeet pass, copy)

> **Executor instructions:** Follow this plan step by step. Run every verification command and confirm the expected
> result before moving on. If anything in "STOP conditions" occurs, stop and report; do not improvise. When done,
> update this plan's row in [`docs/plans/README.md`](README.md) and the M2 row in
> [`spec/README.md#milestones`](../../spec/README.md#milestones).
>
> **Drift check (run first):** written at `bd8cfc7c`, before M1 code existed.
> 1. `grep -n "^| \[003\]\|^| \[010\]" docs/plans/README.md` → 003 must be **IMPLEMENTED** (010 recommended).
> 2. `git diff --stat bd8cfc7c..HEAD -- ChirpKit/Sources/ChirpCore ChirpKit/Sources/ChirpAudio ChirpKit/Sources/ChirpEngineFluidAudio ChirpKit/Sources/ChirpFeatures App/Sources project.yml`
>    then confirm the signatures in "Current state". Refine and commit before coding; STOP if the approach changes.

## Status

- **Milestone:** M2
- **Priority:** P0
- **Effort:** L (capture layer, live session, coordinator, App Intents, a widget extension, the Dictating screen)
- **Risk:** HIGH (real-time audio, interruptions, background rules; the core of the product's daily use)
- **Depends on:** plan 003 IMPLEMENTED; plan 010 recommended (extension and background patterns)
- **Governing docs:** [spec/05 M2 capture](../../spec/05-audio-pipeline.md#m2-capture-for-dictation-proposal),
  [spec/06 routing](../../spec/06-speech-engines.md#scheduling-and-routing), [spec/07](../../spec/07-text-processing.md),
  [design handoff: Dictating](2026-09-22-001-feat-iphone-app-design-handoff.md#screen-dictating-dictatingdchtml-full-screen-dark),
  [speech-engine plug-in contract](../../spec/contracts/speech-engine-plugin-v1.md)
- **Planned at:** commit `bd8cfc7c`, 2026-09-22
- **Status:** NOT STARTED

## Why this matters

Dictation is the owner's most frequent task: press the Action Button, speak a note, get clean text on the clipboard.
MacParakeet's defining rule carries over: live text is only a preview; the text you get comes from a final Parakeet
pass over the recording, which is more accurate. The Gemini port got this backwards (its "final" text was the last
partial), which the review rejected.

## Current state (verified live at `4c2d1734`)

Drift check run 2026-09-22: plan 003 is **IMPLEMENTED**; plan 010 (M1.5) is merged, device checks pending. The diff
`bd8cfc7c..HEAD` over ChirpCore, ChirpAudio, ChirpEngineFluidAudio, ChirpFeatures, App/Sources and `project.yml` is the
M1 + M1.5 code (57 files). Each claim below was checked in the file named; the approach does not change.

- **Scheduler** (`ChirpCore/Scheduling/SpeechJobScheduler.swift`): actor, `run(_ kind:operation:)`; `.dictation` is
  the only kind on the interactive slot and never waits for background jobs; a cancelled caller cancels its pending
  or running job. No test yet races a cancel against a slot grant (carried item below).
- **Engine** (`ChirpEngineFluidAudio/ParakeetEngine.swift`): actor; only `transcribe(fileAt:options:progress:)`
  (16 kHz mono WAV). The internal `ParakeetWorker` protocol has the URL overload only; FluidAudio 0.16.1's
  `AsrManager.transcribe(_ samples: [Float], decoderState:language:)` exists for the preview and the padded short
  final pass. `SpeechTranscriptionOptions` has only `languageHint`. `ModelAssetLifecycle.prepare()` awaits a shared
  load task, so a cancelled waiter only notices after the load ends (carried item below).
- **ChirpAudio**: only `AVAudioNormalizer` (file decoding, track probe). No capture code, no `AVAudioSession` use.
  The only session code in the repo is `App/Sources/Screens/Transcript/PlayerBar.swift`
  (`AVAudioSession.sharedInstance()` `.playback` / `.spokenAudio`, set from the view model; Minor 4).
- **ChirpText**: `TextRefinement().refine(rawText:mode:customWords:snippets:removeUmFiller:)` returns nil for `.raw`;
  `CustomWord`, `TextSnippet`, `KeyAction` are plain models (no GRDB conformance, no persistence).
- **ChirpStore** (`DatabaseManager.swift`): migrations `v1-transcriptions`, `v2-audio-track-ordinal`. The parallel
  M4 lane adds `v3-language-models`, so this plan's migration is named **`v4-dictation-text`**.
- **ChirpFeatures**: `FileTranscriptionPipeline(…, customWords: @escaping @Sendable () -> [CustomWord] = { [] }, …)`
  reads the closure once per `.clean` job and always passes `snippets: []`; `AppEnvironment` passes nothing, so it
  is `{ [] }`. `SettingsStoring` / `TranscriptionSettings` (cleanupMode, speakerLabelsEnabled, parakeetVariant,
  removeUmFiller; forgiving decode) is where "keep dictation audio" lands.
- **Models**: `Transcription.SourceType.dictation` exists; the Library "Dictations" filter exists
  (`LibraryViewModel`). Rows live in the one `transcriptions` table (upstream has a separate `dictations` table).
  `AppEnvironment.retry(_:)` sends every row to the file pipeline, which would diarize a dictation; M2 routes
  dictation rows to the dictation coordinator instead.
- **App**: the Capture Dictate card opens `NotBuiltYetSheet(.dictation)` and shows the "Not built yet · M2" chip;
  Settings → Capture rows (Dictation trigger, Back Tap, Stop mode) and Text → "Custom words & snippets" ("None") are
  placeholders. `project.yml` has one app target and one unit-test target; `Info.plist` has **no**
  `NSMicrophoneUsageDescription`, no `UIBackgroundModes`, no `NSSupportsLiveActivities`, and there is no widget
  extension. Entitlements: none.
- **Upstream** (pinned `bbae9e0e`): `Audio/SharedMicrophoneStream.swift` (fan-out snapshot under a lock, serial
  engine queue, 4096-frame tap, engine-death callbacks), `Audio/MicrophoneEnginePlatform.swift` (macOS HAL/VPIO;
  config-change recovery rebuilds the engine and re-installs the tap), `Audio/AudioRecorder.swift` (channel-0 rule,
  `AVAudioConverter` to 16 kHz mono Float32, 0.3 s minimum), `Services/Dictation/DictationService.swift` (tail
  preview `beginDisplayPreviewIfAvailable`: 1 s interval, 15 s window, defaults at L200–202; stop L472;
  `processCapturedAudio` ~L1370), `STT/STTRuntime.swift` (`paddedDictationSamples`: 0.5 s pad only when the padded
  clip fits one model window, ~L666/L771), `Services/Dictation/LiveTranscriptStabilizer.swift` + tests,
  `DictationFlow/DictationFlowStateMachine.swift` + tests (pure events → effects, generation-guarded).
- **Salvage** (`legacy/gemini-ios/`): `IOSMicrophoneEnginePlatform.swift` + tests, `RecordingActivityAttributes.swift`,
  `ParakeetAppIntents.swift` exist. Re-review found bugs not to copy: `handleMediaServicesReset` calls `prepare`
  (a `queue.sync`) from inside `queue.async` (deadlock), and interruption resume restarts the old engine without
  re-installing the tap.

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Focused tests | `scripts/check.sh ChirpAudioTests` (and `ChirpFeaturesTests`, `ChirpTextTests`) | green, lint clean |
| Full package suite (once) | `swift test --package-path ChirpKit` | 0 failures |
| Simulator | `scripts/test.sh` | `** TEST SUCCEEDED **` |
| Device | `scripts/run_device.sh`; `scripts/device_smoke.sh` | launches; `SMOKE PASS` |

## Scope

- **In scope:** `ChirpAudio` capture (session controller, shared mic stream, recorder), a live-session protocol in
  `ChirpCore` (contract update), tail-window preview in `ChirpEngineFluidAudio`, a dictation coordinator and view
  model in `ChirpFeatures`, `custom_words` and `text_snippets` tables in `ChirpStore`, the Dictating screen, the
  custom words and snippets editor, App Intents + App Shortcuts, one widget extension target for the Live Activity
  and a Control, `project.yml`, specs and contracts.
- **Must not change:** file-transcription behavior; scheduler semantics; the rule that pasted/copied text comes from
  the final pass; the privacy promise (audio stays on device); existing migrations.
- **Out of scope:** keyboard extension (M8), streaming Nemotron/Parakeet EOU engines (M7; design the live-session
  protocol so they can plug in), language-model polish (M4; "Polish after" uses the deterministic Clean pipeline
  until M4), meetings (M3).

## Carried from M0/M1 review

Small items the M0/M1 whole-branch review deferred here because they land naturally with this milestone's work:

- **Step 1** — `PlayerBar.swift` currently drives `AVAudioSession` directly from the view (Minor 4). Route it
  through the `ChirpAudio` session controller this step builds, and add interruption / route-change handling for
  playback so dictation and playback never fight over the session.
- **Step 3** — add `SpeechJobScheduler` tests for a cancel racing a slot grant (the review flagged this as untested,
  not broken).
- A cancelled job that is waiting on a model load already in progress should not appear to hang: confirm the
  cancellation is observed once the load finishes (or make the wait itself cancellable) and add a test.
- Minor 9 (first half): `AppEnvironment.swift` and `SettingsScreen.swift` use the concrete `ParakeetEngine` /
  `FluidAudioDiarizer` types rather than the engine-registry protocols the plug-in contract describes. M2 adds a
  second concrete engine surface (the live-session protocol) — revisit then whether to introduce the registry here
  or defer it to M4, when a second speech-engine provider actually exists to prove the abstraction against.

## Git workflow

Branch `m2/dictation` in its own worktree. Commit after each step with a message that states what now exists. No
assistant trailers. Do not push.

## Steps

### Step 1: Audio session and shared microphone stream (`ChirpAudio`)

Port the semantics of upstream `SharedMicrophoneStream` (one engine per process, fan-out to subscribers, tap buffer
4096 frames) onto iOS, re-reviewing the salvaged `IOSMicrophoneEnginePlatform` and its tests. `AVAudioSession`
`.playAndRecord` activated in the foreground; interruption handling (resume only on `.shouldResume`); route-change
rebuild on `AVAudioEngineConfigurationChange` that **re-installs the tap** (the upstream silent-stall lesson);
media-services reset. Hide `AVAudioSession` behind a protocol so package tests run on the Mac.

**Verify:** `scripts/check.sh ChirpAudioTests` with a fake session platform: interruption began/ended(shouldResume),
route change re-taps, reset recovers.

### Step 2: Recorder

Port `AudioRecorder`: 16 kHz mono Float32 WAV via `AVAudioConverter`, channel-0 rule under voice processing, reject
recordings shorter than 0.3 s, write into `media/<id>/dictation.wav` (additive to the media layout contract; update it).

**Verify:** feed synthetic buffers (from a `say` fixture) through a fake stream; the WAV has the expected duration ±1%.

### Step 3: Live session contract and tail-window preview

Add a `LiveSpeechSession` protocol to `ChirpCore` (begin, append samples, partial text stream, finish, cancel;
display-only). Update [`speech-engine-plugin-v1.md`](../../spec/contracts/speech-engine-plugin-v1.md) additively.
Implement the upstream tail-window preview for Parakeet: every ~1 s transcribe the last ~15 s, single-flight (skip a
tick while one is running), through the scheduler's interactive path, cancelled and drained on stop. Port
`LiveTranscriptStabilizer` (append-only display, last 3 words tentative).

**Verify:** stabilizer tests ported with upstream names; a preview test with a fake engine proves single-flight and
cancel-on-stop.

### Step 4: Dictation coordinator (final pass)

Port the upstream flow as a state machine (`idle → recording → stopping → done | failed | cancelled`): on stop, cancel
the preview, append 0.5 s of silence for short clips, run `.dictation` on the recorded WAV, run `TextRefinement` with
the user's clean-up mode, custom words and snippets, save a `dictation` row (audio kept per a new setting), and copy
the text with `UIPasteboard` (iOS cannot paste into other apps). Empty text → `emptyTranscript` shown as "Didn't catch
that".

**Verify:** `scripts/check.sh ChirpFeaturesTests`: state machine transitions; the copied text equals the final
pass output, not the last partial; cancel leaves no row; failure keeps the audio and offers Retry.

### Step 5: Custom words and snippets

New migration adding `custom_words` and `text_snippets` (upstream fields), stores, and the Settings → Text editor.
The pipeline's `customWords` closure reads the store.

**Verify:** store tests (in-memory DB); Clean mode applies a custom word end to end in a pipeline test.

### Step 6: Dictating screen and Capture wiring

Build the Dictating screen from the handoff: night background, live text (tentative tail dimmed), waveform from real
levels, timer, Cancel / Stop & copy / Polish after. "Polish after" = run Clean for this dictation. Replace the Dictate
card placeholder. Copy correction: "Press the Action Button, or tap to go hands-free."

**Verify:** `scripts/run_sim.sh` screenshots vs the canvas; on the device, dictate 20 s and paste into Notes.

### Step 7: Action Button, App Shortcuts, Live Activity (widget extension)

Add one widget extension target (Live Activity + a Control) in `project.yml` with `NSSupportsLiveActivities = YES`.
A start intent conforming to `AudioRecordingIntent` must **start a Live Activity when recording begins and keep it
while recording**, or iOS stops the recording. Use `supportedModes` (not the deprecated `openAppWhenRun`); try
`.foreground(.immediate)` first. Stop intent ends the activity after the final pass. The Live Activity shows state,
elapsed time and Stop. App Shortcut phrases for "Dictate with Parakeet". The owner assigns the shortcut or Control to
the Action Button in iOS Settings; Settings → Capture explains how (and how to use Back Tap, which is an iOS
Accessibility setting the app cannot set).

**Verify:** on the device: Action Button starts dictation from the Lock Screen and from another app; the Live
Activity appears in the Dynamic Island; Stop copies the final text.

### Step 8: Background and iOS 27

Add `UIBackgroundModes: audio`. Recording must start in the foreground (a Control/intent brings the app forward).
Record in `docs/research/` what happens when the final pass runs while the app is in the background on the owner's
iOS version (Neural Engine on iOS 26; restricted on iOS 27).

## Test plan

- Ported upstream tests (stabilizer, dictation state machine) keep their names.
- Fakes for the session platform, mic stream, engine and store; no real microphone in package tests.
- Device QA checklist (add to [`human-qa-guide.md`](../human-qa-guide.md)): Action Button start/stop; phone call
  during dictation (paused, resumes or ends cleanly, audio kept); AirPods connect mid-dictation; very short press
  (<0.3 s) rejected gracefully; airplane mode works.

## Done criteria

- [ ] Final text always comes from the final pass (test pins it)
- [ ] Action Button → dictate → text on clipboard, from the Lock Screen and from another app (device)
- [ ] Interruptions and route changes never lose the recording (device + fakes)
- [ ] Custom words and snippets editable and applied in Clean mode
- [ ] Contracts and specs updated (live session, media layout additions); focused and full suites green once;
      `scripts/test.sh` green; `scripts/device_smoke.sh` `SMOKE PASS`
- [ ] Everything committed; nothing pushed

## STOP conditions

- The design needs an entitlement or capability beyond the widget extension's plain bundle id (App Groups, Siri,
  push): owner decision.
- `AudioRecordingIntent` cannot start from a Control on the device without opening the app, and no acceptable UX is
  left: report findings with options.
- Any path would make the copied text differ from the final pass output.
- A route-change or interruption test cannot be made deterministic (report timing instead of adding sleeps).

## Maintenance notes

- Keep the preview single-flight; a queue of previews starves the final pass.
- Voice-processing I/O changes channel layout; keep the channel-0 rule from upstream.
- The live-session protocol is the seam for M7 streaming engines; do not special-case Parakeet in the coordinator.
