# Plan: M1.5 — Finish long files in the background; import from the Share sheet and Voice Memos

> **Executor instructions:** Follow this plan step by step. Run every verification command and confirm the expected
> result before moving on. If anything in "STOP conditions" occurs, stop and report; do not improvise. When done,
> update this plan's row in [`docs/plans/README.md`](README.md) and the M1.5 row in
> [`spec/README.md#milestones`](../../spec/README.md#milestones).
>
> **Drift check (run first):** this plan was written at `bd8cfc7c`, before M1 code existed, so its "Current state"
> describes the M1 end state promised by plan 003.
> 1. `grep -n "^| \[003\]" docs/plans/README.md` → must say **IMPLEMENTED**. If not, STOP.
> 2. `git diff --stat bd8cfc7c..HEAD -- project.yml App/Sources ChirpKit/Sources/ChirpFeatures ChirpKit/Sources/ChirpCore`
>    then open the files named in "Current state" and confirm each signature. Refine this plan to match reality and
>    commit the refinement before coding. If a difference changes the approach, STOP.

## Status

- **Milestone:** M1.5
- **Priority:** P0 (first milestone after M1)
- **Effort:** M (app-target work plus one Info.plist change; the optional extension is a second M)
- **Risk:** MEDIUM (background execution and system UI are only testable on the device)
- **Depends on:** plan 003 (M0 + M1) IMPLEMENTED
- **Governing docs:** [spec/05 background execution](../../spec/05-audio-pipeline.md#background-execution),
  [spec/11 share sheet](../../spec/11-ingest.md#share-sheet-m15), [ADR-008](../../spec/adr/008-distribution-and-build-identity.md),
  [media layout contract](../../spec/contracts/media-storage-layout-v1.md)
- **Planned at:** commit `bd8cfc7c`, 2026-09-22
- **Status:** NOT STARTED

## Why this matters

In M1 a long recording stops transcribing the moment the owner switches apps, and the only way in is the file
picker. The owner's real inputs are Voice Memos and files other apps hand them. This milestone makes the two
everyday paths work: share a Voice Memo to Parakeet, lock the phone, and find the finished transcript later, with the
system's own progress UI while it runs.

## Current state (expected after M1)

- `ChirpFeatures.FileTranscriptionPipeline` (actor): `importFile(from:sourceType:) async throws -> UUID` copies into
  `media/<id>/source.<ext>` and inserts a `processing` row; `process(id:) async -> Transcription?` runs the job and
  reports `JobProgress(stage:fraction:)` through `onProgress`.
- `ChirpFeatures.TranscriptionJobCenter` (`@MainActor @Observable`): `start(fileAt:pipeline:)`, `cancel(_:)`,
  `progress[UUID]`.
- `App/Sources/AppEnvironment.swift` builds everything and calls `markStaleProcessingAsInterrupted()` at launch.
- `project.yml`: one app target `iChirp`, no extensions, no entitlements file; Info.plist properties include
  `UIFileSharingEnabled: true`, `LSSupportsOpeningDocumentsInPlace: false`.
- Salvage candidates in `legacy/gemini-ios/`: `Sources/MacParakeetMobileUI/Extensions/ShareExtensionHandler.swift`,
  `Sources/MacParakeetCore/Services/System/DarwinNotificationBroadcaster.swift`, `AppGroupConstants.swift`.
- Upstream reference for multi-track files: `upstream/macparakeet/spec/contracts/file-transcription-audio-tracks.md`.

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Focused tests | `scripts/check.sh ChirpFeaturesTests` | build OK, tests pass, lint clean |
| Full package suite (once, at the end) | `swift test --package-path ChirpKit` | 0 failures |
| Simulator build and app tests | `scripts/test.sh` | `** TEST SUCCEEDED **` |
| Device install | `scripts/run_device.sh` | app launches on the iPhone |
| Device smoke | `scripts/device_smoke.sh` | `SMOKE PASS` |

## Scope

- **In scope:** `App/Sources/**` (background runner, open-URL import), `project.yml` (document types, background
  task identifiers), `ChirpKit/Sources/ChirpFeatures/**` (a background-progress adapter, if logic is needed),
  `ChirpKit/Sources/ChirpAudio/**` (audio-track selection), their tests and READMEs, the specs and contracts named
  above.
- **Corrected 2026-09-22 (final-review I3):** the M0/M1 whole-branch review found that FluidAudio 0.16.1 owns a
  fixed default `URLSession` with no public hook to swap it, so "move model downloads to a background `URLSession`"
  (the Task 14 follow-up below) is not buildable without rewriting FluidAudio's downloader. Do this instead: wrap
  the Settings Download tap in the same `BGContinuedProcessingTask` this milestone already builds for long files
  (Step 2), so the system keeps FluidAudio's normal session running with its own progress UI and Cancel, the same
  way it keeps a long transcription alive. This also retires the app-level `DownloadKeepAlive` idle-timer /
  `beginBackgroundTask` shim added as a stopgap in the M0/M1 fix round (`App/Sources/Support/DownloadKeepAlive.swift`)
  once the continued-processing task covers the same window.
  ~~Task 14 follow-up (superseded above): move Parakeet model downloads to a background `URLSession` so a download
  survives the app being backgrounded or the phone locking mid-fetch.~~ The first on-phone M1 model download timed
  out with zero bytes received ("The request timed out.") — the review traced this to the phone's own network
  reachability at that moment, not backgrounding; see
  [`docs/research/2026-09-22-device-benchmarks.md`](../research/2026-09-22-device-benchmarks.md) (corrected) and
  `final-review.md`'s "Download failure: diagnosis and fix".
- **Must not change:** the pipeline's stage order and error semantics; `SpeechJobScheduler` semantics; the media
  layout (additive only); the `transcriptions` schema except an additive, nullable `audioTrackOrdinal` column in a new
  migration; M1 placeholders for other milestones.
- **Out of scope:** dictation (M2), meetings (M3), links and documents (M5), the keyboard and widgets (M2/M8).

## Carried from M0/M1 review

- The temp-audio sweep (`FileTranscriptionPipeline.sweepOrphanedTemporaryAudio`, run at launch) only protects jobs
  started by this process. It matters here specifically if the Share extension (Step 5) ever runs its own jobs
  rather than handing off to the main app — confirm the sweep still covers (or is duplicated for) whichever process
  actually creates temp audio.
- `ChirpEngineFluidAudio` model assets never unload once loaded, and nothing responds to a memory warning (Minor 3);
  the device benchmarks show a 2.3 GB first-run peak. Since this plan is the first to add sustained background
  execution (Step 2's `BGContinuedProcessingTask`), design the unload path and memory-warning response here rather
  than after background jobs exist.
- Switching Parakeet model version in Settings leaves the previous version's files (~470 MB) on disk with no Delete
  affordance (Minor 9, second half). Worth fixing alongside the unload path above, since both touch
  `ModelAssetLifecycle`'s on-disk bookkeeping.

## Git workflow

Branch `m1.5/share-and-background` in its own worktree from the integration branch. Commit after each step with a
message that states what now exists. No assistant trailers. Do not push.

## Steps

### Step 1: "Open in Parakeet" from the Share sheet (no extension, no account change)

Declare audio and movie document types so Parakeet appears in the Share sheet's app row for Voice Memos and Files:
`CFBundleDocumentTypes` for `public.audio` and `public.movie` (role Viewer, handler rank Alternate) in `project.yml`.
Handle incoming file URLs with SwiftUI `.onOpenURL`: start security-scoped access, call the pipeline's import, then
start processing through `TranscriptionJobCenter`. The system's `Documents/Inbox` copy is a temporary file the app may
remove after the import succeeded (it is not user data; the imported copy in `media/<id>/` is).

**Verify:** `scripts/gen.sh && scripts/test.sh` green; on the device (`scripts/run_device.sh`), Voice Memos → Share →
Parakeet → a Recent row appears and completes.

### Step 2: Long jobs continue in the background (`BGContinuedProcessingTask`)

When the user starts a job (import or share), also submit a `BGContinuedProcessingTaskRequest` from the foreground:

- Identifier derived at run time: `"\(Bundle.main.bundleIdentifier!).transcribe.\(id)"`; declare the wildcard
  `$(PRODUCT_BUNDLE_IDENTIFIER).transcribe.*` in `BGTaskSchedulerPermittedIdentifiers` (SideStore rewrites these, so
  never hard-code the prefix).
- Register the launch handler at submission time (not at app launch). In the handler, map `JobProgress` to the
  task's `Progress` (the system expires tasks that do not report progress), set an `expirationHandler` that cancels
  the job (row → `cancelled` or `interrupted`, never lost), and call `setTaskCompleted(success:)`.
- Strategy `.queue` by default.
- Title and subtitle come from the file's display title; the system shows its own Live Activity with Cancel.

During refinement, confirm in Apple's current documentation whether any `UIBackgroundModes` value is required and
whether the task request needs anything beyond the Info.plist identifiers. If it needs an entitlement or capability,
STOP (account change).

**Verify:** unit-test the progress mapping with a fake pipeline; on the device, import a 20-minute synthetic file
(`say -f` a long text into an `.m4a`), lock the phone, and confirm the job finishes with the system progress UI.

### Step 3: Measure background inference on the owner's iOS version

On iOS 26 the Neural Engine is available in the background. On iOS 27 it is not without
`com.apple.developer.background-tasks.continued-processing.inference`. Record in a dated
`docs/research/<date>-background-inference.md`: iOS version, whether the job continued, throughput in foreground vs
background, and any Core ML error. If the phone is on iOS 27 and inference fails in the background, STOP and report:
the options (request the entitlement = account change; CPU fallback = reload the encoder with CPU compute units) are
the owner's decision.

### Step 4: Choose the audio track for multi-track files

Port the upstream audio-track semantics (one-based for users, zero-based ordinal among audio tracks, a single track
needs no picker, two or more require a choice before any row or work exists). Add `audioTrackOrdinal` (nullable) to
`AudioNormalizing.normalize` through an additive overload, a new migration, and a picker sheet in the app. Write
`spec/contracts/file-transcription-audio-tracks-v1.md` from the upstream contract, adapted.

**Verify:** `scripts/check.sh ChirpAudioTests` with a generated two-audio-track `.mov` fixture (built in `setUp`).

### Step 5 (gated): Share extension with an App Group inbox

Only if Steps 1–2 leave a real gap (for example sharing URLs, or importing without opening the app) **and** the owner
approves the account change: an explicit App ID with the App Groups capability for the app and the extension. With
approval, add target `iChirpShare` in `project.yml`, reuse the salvaged `ShareExtensionHandler` logic, derive the App
Group ID at run time, load no models in the extension, and write a contract for the inbox manifest. Weigh it against
the extension budget in [ADR-008](../../spec/adr/008-distribution-and-build-identity.md).

## Test plan

- `ChirpFeaturesTests`: background progress mapping (monotonic 0…1, completion on success, cancellation on
  expiration), import-from-URL path using a temp file.
- `ChirpAudioTests`: track selection on a generated multi-track movie; single-track files unchanged.
- `ChirpStoreTests`: the new migration adds a nullable column and old rows decode.
- Device: Share from Voice Memos; lock-screen continuation; force-quit mid-job → Interrupted + Retry.

## Done criteria

- [ ] Voice Memos → Share → Parakeet imports and transcribes (device)
- [ ] A 20-minute file finishes with the phone locked on iOS 26 (device), with the system progress UI
- [ ] Background inference findings recorded in `docs/research/`
- [ ] Multi-track picker works; contract written; migration additive
- [ ] `scripts/check.sh` focused runs green; full package suite green once; `scripts/test.sh` green;
      `scripts/device_smoke.sh` prints `SMOKE PASS`
- [ ] spec/02, spec/05, spec/11 and the M1.5 milestone row updated; placeholders for later milestones untouched
- [ ] Everything committed; nothing pushed

## STOP conditions

- Plan 003 is not IMPLEMENTED, or the pipeline API differs in a way that changes the approach.
- Any step needs an App ID, capability, entitlement or profile change (App Groups, background inference, increased
  memory). The owner decides.
- On iOS 27, background inference fails and there is no owner decision on the fallback.
- A background job can end in a state other than `completed`, `failed`, `cancelled` or `interrupted` (data loss).
- The phone is not available for a required device check.

## Maintenance notes

- Background task identifiers and App Group IDs are always derived from the running bundle id (SideStore renames them).
- The Inbox copy made by iOS is temporary; the imported `media/<id>/source.<ext>` is the user's data.
- If Apple changes the continued-processing API, keep the job's own state machine authoritative: the system task is
  only a keep-alive and progress surface.
